<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
</p>

<div>
  <ul>
    <% @post.tags.each do |t| %>
      <li><%= t.name %></li>
    <% end %>
  </ul>
</div>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
